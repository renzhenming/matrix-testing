<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rzm.matrix_testing">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:name=".MyApplication"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Matrix_testing">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".io.TestIOActivity"/>
        <activity android:name=".trace.TestStartUpActivity"/>
        <activity android:name=".trace.TestOtherProcessActivity"/>
        <activity android:name=".trace.TestTraceMainActivity" />
        <activity android:name=".trace.TestTraceFragmentActivity" />
        <activity android:name=".trace.TestEnterActivity" />
        <activity android:name=".trace.TestFpsActivity" />
    </application>

</manifest>